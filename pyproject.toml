[project]
name = "novavision-demo-package"
version = "0.1.0"
description = "NovaVision demo package with two executors and dependent dropdown configs."
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2,<3"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

